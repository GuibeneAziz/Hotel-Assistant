# One-shot seed image (runs before app on docker compose up)
FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache libc6-compat
COPY package.json package-lock.json ./
RUN npm ci --omit=dev
COPY scripts ./scripts
CMD ["node", "scripts/docker-seed-if-empty.mjs"]
